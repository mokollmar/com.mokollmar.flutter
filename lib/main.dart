import 'package:com_mokollmar/app.dart';
import 'package:flutter/material.dart';

void main() {
  runApp(const App());
}

//! flutter build web


// HTML => small donwload size, uses a combination of HTML elements, CSS, Canvas elements, and SVG elements
// CanvsKit => big donwload size (1.5mb), fully consistent with Flutter mobile and desktop, has faster performance with higher widget density